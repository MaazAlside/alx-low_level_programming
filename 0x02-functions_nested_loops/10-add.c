#include <stdio.h>
#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @a : the frist number to add
 * @b : thr second number to add
 * Return: trturn a + b
 */

int add(int a, int b)
{
	return (a + b);
}
