#include <stdio.h>
#include "main.h"

/**
 * print_last-digit -  function that prints the last digit of a number.
 * @a : an integer paramenter
 * Return : return last digit
 */

int print_last_digit(int a)
{
/*	int lastDigit;
//	lastDigit = a % 10;

//	_putchar((lastDigit) + '0');*/
	return a % 10;
}
